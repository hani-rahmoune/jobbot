# M8 found Workday genuinely public and unauthenticated; every other
# enterprise ATS vendor checked that milestone (iCIMS, SAP SuccessFactors,
# Oracle Taleo, Cegid Talentsoft, Phenom People, Oleeo) was rejected on the
# strength of vendor documentation and a couple of quick probes.
#
# M9 re-investigated all of those properly -- checking what each real
# careers page's OWN request actually does, not just what the vendor
# documents -- and found three of them genuinely public and unauthenticated
# after all: SmartRecruiters, iCIMS's "Jibe" career-site product
# specifically (NOT "classic" iCIMS, which is still rejected -- see
# jobbot/sources/jibe.py's module docstring), and Cegid Talentsoft (via its
# server-rendered candidate-facing listing page, not its authenticated
# REST API). SAP SuccessFactors, Phenom People, Oleeo, Avature, and Oracle
# Taleo were all re-checked for M9 too and confirmed still not viable --
# see the M9 report for the specific reason each one failed.
#
# Every entry below was verified live before being added: fetched its real
# endpoint/page and confirmed real, current postings came back (not just an
# HTTP 200). tier: warm, not hot -- Workday boards in particular can run to
# 2000+ postings per employer and are correspondingly expensive to poll
# often; each adapter's own max_postings/page caps apply regardless of tier.
#
# M9b re-swept the two working vendors (Workday, SmartRecruiters) against a
# much longer list of large French employers, and re-investigated all four
# vendors M9 had rejected, trying harder per CLAUDE.md's Part D playbook
# before concluding again. One more rejected vendor turned out to have a
# genuinely usable path after all: Phenom People's own search widget
# (Orange, Thales) is still confirmed session/CSRF-bound and unusable, but
# both employers' individual job pages -- discoverable through a real,
# robots.txt-listed sitemap -- carry complete, valid schema.org JobPosting
# JSON-LD. That's a different shape of problem than jsonld.py solves (many
# discovered single-posting pages, not one page with many postings
# embedded), so it's a new adapter, not a reuse of jsonld.py --
# see jobbot/sources/sitemap_jsonld.py's module docstring. SAP
# SuccessFactors, iCIMS classic, and Oleeo were all re-confirmed rejected,
# each for a more precise reason than before -- see the M9b report.
#
# Identifier formats (see each adapter module's own docstring for the full
# reasoning):
#   workday          "{tenant}.wd{N}.{site}"
#   smartrecruiters  the bare SmartRecruiters company identifier
#   jibe             the employer's Jibe domain, optionally "|{country}"
#                    for a board that spans more than one country
#   talentsoft       the employer's own Talentsoft subdomain (https URL)
#   sitemap_jsonld   the employer's sitemap index URL (from robots.txt's
#                    own "Sitemap:" line)

- name: Renault Group
  ats: workday
  identifier: alliancewd.wd3.renault-group-careers
  tier: warm
  tags: [automotive, cac40, workday]

- name: Stellantis
  ats: workday
  identifier: stellantis.wd3.External_Career_Site_ID01
  tier: warm
  tags: [automotive, workday]

- name: Sanofi
  ats: workday
  identifier: sanofi.wd3.SanofiCareers
  tier: warm
  tags: [pharma, cac40, workday]

- name: Michelin
  ats: workday
  identifier: michelinhr.wd3.Michelin
  tier: warm
  tags: [manufacturing, cac40, workday]

- name: Airbus
  ats: workday
  identifier: ag.wd3.Airbus
  tier: warm
  tags: [aerospace, cac40, workday]

- name: Veolia
  ats: workday
  identifier: veoliauki.wd3.VESCareers
  tier: warm
  tags: [utilities, cac40, workday]

# M9: SmartRecruiters. Verified live 2026-09-02, total postings and the
# server-side "alternance"/"stage" search narrowing (`q` query param) --
# see jobbot/sources/smartrecruiters.py's module docstring for the
# mechanism. Identifier is SmartRecruiters' own company token, the segment
# right after careers.smartrecruiters.com/ on that employer's page.

- name: Kiabi
  ats: smartrecruiters
  identifier: KIABI
  tier: warm
  tags: [retail, smartrecruiters]
  # 267 total; "alternance" 8, "stage" 21

- name: Boulanger
  ats: smartrecruiters
  identifier: Boulanger
  tier: warm
  tags: [retail, smartrecruiters]
  # 209 total; "alternance" 13, "stage" 1

- name: Ubisoft
  ats: smartrecruiters
  identifier: Ubisoft2
  tier: warm
  tags: [tech, smartrecruiters]
  # 284 total; "alternance" 4, "stage" 4

# M9: iCIMS Jibe. Verified live 2026-09-02. AXA's board is genuinely
# global under one domain, hence the "|France" country filter in the
# identifier -- see jobbot/sources/jibe.py's module docstring.

- name: AXA
  ats: jibe
  identifier: https://careers.axa.com|France
  tier: warm
  tags: [insurance, cac40, jibe]
  # ~560-600 France postings; "alternance" (keywords param) 11

# M9: Cegid Talentsoft. Verified live 2026-09-02, total postings and the
# server-side "alternance"/"stage" search narrowing (`Keywords` query
# param) -- see jobbot/sources/talentsoft.py's module docstring, including
# why LCL and CACIB render two different (but both supported) listing
# templates despite being the same Credit Agricole group and platform.

- name: Credit Agricole CIB
  ats: talentsoft
  identifier: https://casa-cacib-recrute.talent-soft.com
  tier: warm
  tags: [banking, cac40, talentsoft]
  # 313 total; "alternance" 45, "stage" 37, "apprentissage" 9

- name: LCL
  ats: talentsoft
  identifier: https://casa-lcl-recrute.talent-soft.com
  tier: warm
  tags: [banking, talentsoft]
  # 193 total; "alternance" 2, "stage" 1, "apprentissage" 2

# M9b: more Workday tenants, found by checking each named employer's real
# careers page for a myworkdayjobs.com URL and verifying live -- see the
# M9b report's table for every name checked, including the ones that
# turned out NOT to be on Workday. "stage" is unusually noisy for the two
# pharma/mining employers here (Pierre Fabre, Imerys) -- Workday's fuzzy
# search matches "stage" against ordinary industry vocabulary ("clinical
# stage", "multi-stage process"), not just the French internship word --
# kept anyway since "alternance" stays a clean, low-noise signal for both.

- name: Ipsen
  ats: workday
  identifier: ipsen.wd103.Ipsen_Careers
  tier: warm
  tags: [pharma, workday]
  # 264 total; "alternance" 12, "stage" 80

- name: Pierre Fabre
  ats: workday
  identifier: pierrefabre.wd3.External_Career_Site
  tier: warm
  tags: [pharma, workday]
  # 210 total; "alternance" 7, "stage" 132 (noisy, see above)

- name: Pernod Ricard
  ats: workday
  identifier: pernodricard.wd3.pernod-ricard
  tier: warm
  tags: [consumer, cac40, workday]
  # 263 total; "alternance" 35, "stage" 45

- name: Imerys
  ats: workday
  identifier: imerys.wd3.IMERYS-Careers
  tier: warm
  tags: [manufacturing, workday]
  # 118 total; "alternance" 12, "stage" 118 (noisy, see above)

- name: CNP Assurances
  ats: workday
  identifier: lmg.wd3.CNPPS
  tier: warm
  tags: [insurance, workday]
  # 36 total; "alternance" 3, "stage" 4

# M9b: more SmartRecruiters companies, same verification method as above.

- name: Sopra Steria
  ats: smartrecruiters
  identifier: SopraSteria1
  tier: warm
  tags: [tech, smartrecruiters]
  # 1971 total; "alternance" 9, "stage" 69

- name: Somfy
  ats: smartrecruiters
  identifier: somfygroup
  tier: warm
  tags: [manufacturing, smartrecruiters]
  # 61 total (mostly international); "alternance" 2, "stage" 3 -- real
  # France postings confirmed present despite the small France share

- name: Lacroix
  ats: smartrecruiters
  identifier: LACROIX1
  tier: warm
  tags: [manufacturing, smartrecruiters]
  # 52 total; "alternance" 1, "stage" 0

# M9b: more Cegid Talentsoft tenants, found the same way (a company's own
# careers page linking to its "-recrute.talent-soft.com" subdomain).

- name: Naval Group
  ats: talentsoft
  identifier: https://dcns-recrute.talent-soft.com
  tier: warm
  tags: [defense, talentsoft]
  # 382 total; "alternance" 5, "stage" 4

# M11 B2: re-tested after M10 wrongly rejected these for "no JobPosting
# JSON-LD in the sample" -- both are Talentsoft tenants, not sitemap_jsonld
# candidates at all. Sodebo's is on the standard *.talent-soft.com domain;
# MAIF's is a custom vanity domain (recrutement.maif.fr) that Talentsoft
# supports for enterprise tenants -- same /job/list-of-all-jobs.aspx
# endpoint, same ts-offer-card/ts-offer-list-item markup, confirmed live.
# The existing talentsoft adapter needed no changes.

- name: Sodebo
  ats: talentsoft
  identifier: https://sodebo-career.talent-soft.com
  tier: warm
  tags: [food, talentsoft]
  # 28 total, confirmed live; robots.txt carries no rules (all allowed)

- name: MAIF
  ats: talentsoft
  identifier: https://recrutement.maif.fr
  tier: warm
  tags: [insurance, talentsoft]
  # 37 total, confirmed live (custom domain, not *.talent-soft.com);
  # robots.txt carries no rules (all allowed)

# M9b: sitemap_jsonld -- Phenom People's search widget stays rejected
# (session/CSRF-bound), but both employers' job pages are reachable this
# way. Identifier is the sitemap index URL from each site's own robots.txt.
# Totals below are total discoverable job URLs across every per-locale
# sub-sitemap, not postings actually fetched each poll -- search_terms,
# then the slug vocabulary, then a sample narrows that down to a bounded
# set per employer (see jobbot/sources/sitemap_jsonld.py's module
# docstring and DEFAULT_PAGE_CAP).

- name: Thales
  ats: sitemap_jsonld
  identifier: https://careers.thalesgroup.com/fr/fr/sitemap_index.xml
  tier: warm
  tags: [aerospace, defense, cac40, sitemap_jsonld]
  # 2635 total job URLs across 6 sub-sitemaps; "alternance" 33, "stage" 12,
  # "internship" 12 (URL-slug matches, before search_terms dedup/fetch)

- name: Orange
  ats: sitemap_jsonld
  identifier: https://orange.jobs/fr/fr/sitemap_index.xml
  tier: warm
  tags: [telecom, cac40, sitemap_jsonld]
  # 657 total job URLs across 2 sub-sitemaps; "alternance" 5, "stage" 6

# M9d: sitemap sweep across every previously-unverified employer (see the
# M9d report's table for the full list checked, including domains that
# genuinely had no sitemap, no job-like URLs, or job-like URLs with no
# JobPosting JSON-LD -- none of those are listed here, only the real hits).

- name: Accor
  ats: sitemap_jsonld
  identifier: https://careers.accor.com/sitemap.xml
  tier: warm
  tags: [hospitality, sitemap_jsonld]
  # search_terms narrowed to 86 real candidate job URLs (the confirmed live
  # fetch hit the 60-page cap and returned exactly 60 -- see the M9d report)

- name: Rexel
  ats: sitemap_jsonld
  identifier: https://careers.rexel.com/sitemap.xml
  tier: warm
  tags: [distribution, sitemap_jsonld]
  # search_terms narrowed to 18 real postings fetched, confirmed live
  # (e.g. "Stage Datacom & Security Specialist", real "stage" content)

# M11 B1: added after Part A's slug-vocabulary fallback fix. M10 rejected
# this employer for "zero slug matches" because its sitemap uses
# WordPress-plugin URLs with no French search_terms in the slug at all;
# the new DEFAULT_SLUG_VOCABULARY's "stage" substring-matches "stagiaire"
# and got through.

- name: Manitou
  ats: sitemap_jsonld
  identifier: https://careers.manitou-group.com/sitemap_index.xml
  tier: warm
  tags: [industrial, sitemap_jsonld]
  # slug_vocabulary path matched 3 of 84 job-like URLs, confirmed live:
  # "Stagiaire Marketing Produit", Ancenis FR -- real posting, real content.

# M11 B1: Geodis investigated and NOT added. The domain carried forward
# from M10's notes as "confirmed live" (workatgeodis.com) is a genuine
# sitemap_jsonld source, but live-verification this session shows it is
# GEODIS's North America subsidiary board -- 256 job-like URLs, all
# US/Canada warehouse and logistics roles, zero slug-vocabulary matches
# (no stage/alternance/intern/trainee/vie anywhere), so it would never
# surface a French posting. GEODIS's actual French job board is
# https://injob.geodis.com (linked from geodis.com/fr/carriere), a
# proprietary ASP.NET portal -- no sitemap.xml (404), no ld+json on the
# pages checked. It doesn't fit this adapter; a bespoke adapter would be
# needed, out of scope for M11. Recorded here so no future session
# re-guesses workatgeodis.com for this company.

# M11 B2: re-tested against the M10 rejection list with a wider net --
# Scaleway's own careers page links straight to a standard Lever board,
# not sitemap_jsonld at all. Confirmed live via the real LeverSource:
# 38 postings, including a real internship ("Approvisionneur - Stage",
# Paris). jobs.lever.co's robots.txt allows crawling (the only
# Content-Signal opt-out is ai-train, which doesn't apply to relaying
# postings to Discord).

- name: Scaleway
  ats: lever
  identifier: scaleway
  tier: warm
  tags: [cloud, tech, lever]

# M12 Part B: Danone (careers.danone.com) is NOT SAP SuccessFactors, despite
# an earlier session's identification. Its job pages link straight to
# https://frapply-danone.icims.com/jobs/{id}/login -- the id matches the
# job's own URL slug exactly. Danone's real ATS backend is iCIMS, fronted by
# Adobe Experience Manager (its CSP header is full of *.adobeaemcloud.com,
# zero successfactors/sapsf/jobs2web references anywhere -- confirmed by
# direct inspection, not inferred from the URL shape, which is what caused
# the original misidentification). iCIMS is already a known-rejected vendor
# (M9b). Do not re-attempt Danone via successfactors.py or re-investigate
# this as an open question; it's closed. See the M12 report for the full
# three-independent-signal method that ruled SuccessFactors out.
#
# M12 Part B4: probed discovery/probe_rmk.py against every resolved domain
# for these employers and found NO RMK signal (checked, not just unresolved)
# -- recorded here so a future session doesn't re-probe them: Safran
# (safran-group.com), Schneider Electric (careers.se.com), Saint-Gobain
# (joinus.saint-gobain.com, no robots.txt/sitemap at all -- likely a
# Drupal-hosted page with a separate embedded widget, not investigated
# further), Groupama (groupama-gan-recrute.com), BPCE (recrutement.bpce.fr),
# Natixis (recrutement.natixis.com), Enedis (enedis.fr), Elior
# (eliorgroup.com), Groupe SEB (groupeseb-careers.com, 404s), Covea
# (recrutement.covea.com -- actually Cornerstone OnDemand, csod.com, per its
# own linked application portal), Decathlon (recrutement.decathlon.fr and
# joinus.decathlon.fr), L'Oreal (career.loreal.com), Michelin
# (recrutement.michelin.fr), Hermes (talents.hermes.com), Amadeus
# (jobs.amadeus.com / amadeus.com), Legrand (careers.legrand.com -- the real
# electrical-equipment company, not the wrong-entity carrieres.groupe-legrand.fr
# from M11), Dassault Systemes (3ds.com), La Poste (laposterecrute.fr),
# Kering (kering.com), TotalEnergies (jobs.totalenergies.com -- confirmed
# Avature, see M11), Credit Agricole SA (groupecreditagricole.jobs), Societe
# Generale (careers.societegenerale.com). Bouygues, Vinci, Suez, Intermarche,
# and Accenture France had no single clean careers domain resolve at all
# this session (aggregator results only, or a per-entity split with no one
# obvious host) -- genuinely unresolved, not checked-negative.
#
# Confirmed RMK tenants from this sweep are added below with an
# `ats: successfactors` entry each; see jobbot/sources/successfactors.py.
#
# M13 Part A correction: M12 wrote off Capgemini, Alstom, and Sephora
# together as "sitemap.xml is an RSS feed, not usable" -- true of the format,
# but M12 never actually checked Alstom/Sephora's own item counts, only
# inferred "broken like Capgemini" from the shared shape. They aren't: RSS
# mode is now a real, tested second discovery path (see
# jobbot/sources/successfactors.py's module docstring), and Alstom/Sephora's
# feeds are large and genuinely current (2000+ items each, both added
# below). Capgemini's own feed is a real, separate problem -- confirmed
# stuck at exactly one item by every parameter, path, and alternate host
# tried this session (see the module docstring's RSS MODE section for the
# full list) -- and stays excluded. Nexans stays excluded too, for its own
# reason: fully client-rendered, belongs to rendered.py (M13 Part C).

- name: Eramet
  ats: successfactors
  identifier: https://jobs.eramet.com/sitemap.xml
  tier: warm
  tags: [mining, metallurgy, successfactors]
  # slug_vocabulary path matched 19 of 66 job-like URLs, confirmed live:
  # multiple real "Stagiaire..." postings, Owendo GA (Gabon operations).

- name: Worldline
  ats: successfactors
  identifier: https://jobs.worldline.com/sitemap.xml
  tier: warm
  tags: [payments, tech, successfactors]
  # slug_vocabulary path matched 45 of 317; confirmed live, including
  # "Alternance - Group Benefits F/H", Puteaux -- the same posting M11/M12
  # found redirecting to an en_US-suffixed URL, location correctly resolved
  # from the URL slug fallback since this tenant carries no streetAddress
  # itemprop (the lenient-extraction path this adapter exists for).

- name: Atos
  ats: successfactors
  identifier: https://jobs.atos.net/sitemap.xml
  tier: warm
  tags: [tech, consulting, successfactors]
  # slug_vocabulary path matched 22 of 967; confirmed live, real French
  # "Stage" postings in Courbevoie and Toulouse.

- name: Bel
  ats: successfactors
  identifier: https://jobs.groupe-bel.com/sitemap.xml
  tier: warm
  tags: [food, successfactors]
  # slug_vocabulary path matched 83 of 258; confirmed live, real French
  # "Alternant(e)"/"Stage" postings.

- name: Lactalis
  ats: successfactors
  identifier: https://jobs.lactalisexperience.fr/sitemap.xml
  tier: warm
  tags: [food, successfactors]
  # slug_vocabulary path matched 41 of 297; confirmed live, real French
  # "Alternant" postings with streetAddress-derived locations.

- name: Arkema
  ats: successfactors
  identifier: https://jobs.arkema.com/sitemap.xml
  tier: warm
  tags: [chemicals, successfactors]
  # slug_vocabulary path matched 33 of 228; confirmed live, real intern
  # postings (international -- French ones exist in the fuller candidate
  # set, not shown in the sample checked).

- name: Solvay
  ats: successfactors
  identifier: https://careers.solvay.com/sitemap.xml
  tier: warm
  tags: [chemicals, successfactors]
  # slug_vocabulary path matched 11 of 70; confirmed live, real intern
  # postings.

- name: Servier
  ats: successfactors
  identifier: https://jobs.servier.com/sitemap.xml
  tier: warm
  tags: [pharma, successfactors]
  # slug_vocabulary path hit the 150-page cap (259 real candidates matched,
  # by far the largest of this sweep) -- confirmed live.

# M13 Part A: RSS-mode tenants, confirmed live via the adapter's new second
# discovery mode. Both feeds are the FULL board, not narrowed by
# search_terms/slug_vocabulary/page_cap (RSS mode has no per-URL fetch cost
# to control -- see the module docstring) -- filters.yaml does the real
# narrowing downstream, same as every other bulk-listing adapter.

- name: Alstom
  ats: successfactors
  identifier: https://jobsearch.alstom.com/sitemap.xml
  tier: warm
  tags: [rail, industrial, successfactors]
  # RSS mode, ~2300 items, confirmed live: real French "Alternance"/"Stage"
  # postings (Rouen, Ornans, Saint-Ouen), correctly classified.

- name: Sephora
  ats: successfactors
  identifier: https://jobs.sephora.com/sitemap.xml
  tier: warm
  tags: [retail, beauty, successfactors]
  # RSS mode, ~1900 items, confirmed live: real French "Alternance"/"Stage"
  # postings. Note: a VIE role's <g:location> field names the sponsoring
  # French entity's country code, not the physical work location (a real
  # Chicago-based VIE reads "Chicago, FR") -- a genuine source convention,
  # relayed as-is rather than corrected, same principle as CLAUDE.md's
  # source-dates rule.

# M13 Part C: rendered.py investigated live against 5 candidates, in the
# given priority order. None added -- every one is genuinely blocked, not
# just untried, and none violate the "no captcha-solving, no Cloudflare
# evasion" rule; they're recorded here so a future session doesn't re-spend
# this same investigation:
#
# - TotalEnergies (jobs.totalenergies.com): its OWN robots.txt disallows
#   every locale-prefixed path (/en_US/*, /fr_FR/*) and even the bare root
#   "/" -- confirmed directly with Python's stdlib robotparser, not just
#   this project's RobotsCache wrapper. The only nominally-Allowed paths
#   (/careers, /careers/SearchJobs) immediately 302 to a Disallowed
#   locale-prefixed one. The 984 slug-matched candidate URLs identified
#   earlier are real, but every one of them lives under a Disallowed path.
#   A genuine robots.txt wall, not a rendering problem.
# - Nexans (career.nexans.com): confirmed RMK, robots.txt allows both
#   /viewalljobs/ and individual /job/ pages. A REAL browser (not just
#   static HTML) still shows literal unresolved `${translations[locale]...}`
#   placeholders and a generic "Job Details" title on every page checked,
#   even after networkidle, an extra 3s settle, and dismissing the cookie
#   banner. Genuinely broken client-side rendering on this tenant, not a
#   missing selector.
# - Capgemini (jobs.capgemini.com): robots.txt allows /viewalljobs/, and
#   the rendered page DOES report "Résultats 1 à 24 sur 24" -- real results
#   exist -- but no corresponding result rows ever materialize in the DOM
#   (0 job links found by any selector tried), even after dismissing the
#   cookie banner. Whatever renders the actual rows didn't fire in this
#   session's checks; not resolved.
# - Saint-Gobain (joinus.saint-gobain.com): returns a Cloudflare challenge
#   page ("Just a moment...", HTTP 403) to a plain page load. Excluded per
#   the explicit rule against defeating Cloudflare -- not attempted further.
# - Kering: NOT actually a rendered.py candidate. Its careers page links to
#   kering.wd3.myworkdayjobs.com, which looked like a Workday tenant, but
#   the real Workday CXS API (kering.wd3.myworkdayjobs.com/wday/cxs/kering/
#   Kering/jobs) returns total:0 -- that link is some other portal (an
#   employee/internal one, not the public board). The REAL public postings
#   (e.g. careers.kering.com/careers/R170165) are on a DIFFERENT, previously
#   unseen vendor: Eightfold AI (confirmed via response headers -- X-EF-*,
#   a CSP referencing eightfold-gov.ai). robots.txt allows /careers. Direct
#   navigation to a job URL in a real browser renders a generic "Jobs" H1
#   rather than the real title, suggesting Eightfold's own client-side
#   routing expects in-app navigation rather than a fresh page load -- not
#   resolved this session. Worth a dedicated future investigation (a new
#   vendor, not folded into this rendered.py sweep): confirm whether
#   Eightfold has its own JSON-LD/API surface before reaching for
#   rendered.py's selector fallback again.
